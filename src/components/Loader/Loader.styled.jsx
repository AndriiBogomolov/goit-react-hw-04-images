import styled from '@emotion/styled';
import { CirclesWithBar } from 'react-loader-spinner';

export const LoaderStyles = styled(CirclesWithBar)`
  display: flex;
  justify-content: center;
  margin-top: 50px;
`;